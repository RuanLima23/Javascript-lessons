// Temos dois tipos de string

// Tipo primitivo
const mensagem = 'Olá mundo!';
// Tipo objeto
const mensagem2 = new String('Olá mundo!');

//Métodos do string: https://developer.mozilla.org/pt-BR/docs/Web/JavaScript/Reference/Global_Objects/String

// Comandos no console:
    //typeof mensagem
    //mensagem2.length: quantas letras tem essa variável
    //mensagem2[2]: qual letra está nessa posição
    //mensagem.includes('mundo'): true (sim, essa palavra existe nessa variável)
    //mensagem.startsWith('Olá'): true (sim, essa variável incia com essa palavra)
    //mensagem.endsWith('!'): true (sim, termina com essa palavra)
    //mensagem.indexOf('mundo'): conta as letras de cada palavra
    //mensagem.replace('mundo','universo'): substitui palavra
    //mensagem.trim(): tira os espaços excedentes
    //mensagem.split(' '): separa a cada pedaço
     